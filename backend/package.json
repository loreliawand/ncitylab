{
  "name": "ncitylab_backend",
  "version": "1.0.0",
  "description": "Backend of the site",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "\"echo \\\"No test specified\\\" && exit 1\"",
    "build": "rm -rf build && cd ../frontend/ && npm run build && cp -r build ../backend"
  },
  "author": "Lora_W",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
